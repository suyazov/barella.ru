# Object pages acceptance v2

Acceptance requires:

- `/obekty/` contains exactly 10 object cards and no expanded case body.
- Every card links to its own route listed in `docs/object-page-slugs-v2.md`.
- Every object page contains its exact title, address, customer and work description from `docs/requirements-objects-v2.md`.
- Every page uses only its own `materials/objects/<object>/` files.
- No placeholder text, no external runtime image source.
- Pskov objects 07/08 and POLISAN objects 06/09 remain separate.
- Responsive desktop/mobile QA; no large white gaps in galleries; consistent aspect-ratio tiles/lightbox or equivalent usable gallery.
- Back link/breadcrumb to `/obekty/`; CTA to contact/form may be reused but must not replace object content.
